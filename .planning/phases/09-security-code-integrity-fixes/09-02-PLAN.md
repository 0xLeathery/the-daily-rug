---
phase: 09-security-code-integrity-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/api/helius-webhook.test.ts
autonomous: true
gap_closure: true
requirements:
  - CHAIN-04
  - CHAIN-02
  - CHAIN-03
  - ADMIN-03
  - AUTH-01

must_haves:
  truths:
    - "All 16 helius-webhook tests pass (npx vitest run tests/api/helius-webhook.test.ts exits 0)"
    - "makeArticleKilledEvent() amount mock uses toNumber() matching the route's .toNumber() call at route.ts line 81"
    - "The 'records burn details' test asserts burned_amount as a number (not a string)"
  artifacts:
    - path: "tests/api/helius-webhook.test.ts"
      provides: "Fixed BN-mock for amount field; all 16 tests green"
      contains: "toNumber: () => 500000000"
  key_links:
    - from: "tests/api/helius-webhook.test.ts makeArticleKilledEvent()"
      to: "app/api/webhooks/helius/route.ts line 81"
      via: "killedEvent.data.amount.toNumber()"
      pattern: "toNumber.*500000000"
---

<objective>
Fix the pre-existing test mock mismatch that causes 11 of 16 helius-webhook tests to fail.

Purpose: The helius-webhook test suite cannot verify CHAIN-04 (Helius webhook updates Supabase on ArticleKilled event) while 11 tests fail with `TypeError: killedEvent.data.amount.toNumber is not a function`. The route (line 81) calls `.toNumber()` on the BN-like amount field, but the test helper `makeArticleKilledEvent()` only provides `.toString()`. This was introduced in commit `1dfc909` (which added `.toNumber()` to the route) without updating the test mocks.
Output: All 16 helius-webhook tests green; CHAIN-04 test coverage fully restored.
</objective>

<execution_context>
@/Users/annon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/annon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-security-code-integrity-fixes/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix amount mock and burned_amount assertion in helius-webhook tests</name>
  <files>tests/api/helius-webhook.test.ts</files>
  <action>
Two targeted edits to tests/api/helius-webhook.test.ts:

**Edit 1 — Fix makeArticleKilledEvent() helper (line 78):**
Change the amount mock from toString-only to toNumber-only to match the route's call pattern:

```typescript
// BEFORE (line 78):
amount: { toString: () => '500000000' }, // BN-like

// AFTER:
amount: { toNumber: () => 500000000 }, // BN-like — matches route's .toNumber() call
```

The route at line 81 of helius/route.ts calls:
  `const amountBurned = (killedEvent.data.amount as { toNumber(): number }).toNumber()`

The null/undefined amount guard at lines 68-73 of route.ts uses falsy check only — `!killedEvent.data.amount` — so undefined and null overrides in specific tests (e.g., `makeArticleKilledEvent({ amount: undefined })`) still reach the guard correctly. Those tests pass amount: undefined explicitly in overrides, which supersedes the default and triggers the skip path before .toNumber() is ever called. No change needed for those tests.

**Edit 2 — Fix burned_amount assertion (line 261):**
The "records burn details" test asserts `burned_amount: '500000000'` (a string). After the mock fix, the route stores `burned_amount: amountBurned` where `amountBurned = .toNumber()` = number 500000000. Update:

```typescript
// BEFORE (line ~261):
burned_amount: '500000000',

// AFTER:
burned_amount: 500000000,
```

These are the only two changes needed. Do NOT add .toString() to the mock — the route does not call .toString() on amount. Do NOT change any other test assertions.
  </action>
  <verify>
    <automated>cd /Users/annon/projects/TMZolana && npx vitest run tests/api/helius-webhook.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All 16 helius-webhook tests pass (0 failures). Output shows "16 passed" with no TypeError.</done>
</task>

</tasks>

<verification>
Run the full API test suite to confirm no regressions:

```bash
cd /Users/annon/projects/TMZolana && npx vitest run tests/api/
```

All tests in tests/api/ must pass: helius-webhook.test.ts (16), presigned-url.test.ts (4), and any others.
</verification>

<success_criteria>
- 16/16 helius-webhook tests pass
- No TypeError: killedEvent.data.amount.toNumber is not a function
- presigned-url tests still pass (4/4 — no regressions from unrelated file)
- npx vitest run exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-code-integrity-fixes/09-02-SUMMARY.md`
</output>
