import { describe, test } from 'vitest'

describe('Auth Trigger: on_auth_user_created', () => {
  test.todo('creates a profile row when a new auth user is created')
  test.todo('sets display_name from user metadata or falls back to email')
  test.todo('sets default role to editor')
  test.todo('sets is_agent from user metadata')
})
