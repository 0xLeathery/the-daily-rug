import { describe, test } from 'vitest'

describe('Tombstone Burn Credit', () => {
  test.todo('renders burn credit line with truncated wallet address')
  test.todo('renders burn amount formatted with K/M suffix')
  test.todo('does not render burn credit when burned_by is null')
})
