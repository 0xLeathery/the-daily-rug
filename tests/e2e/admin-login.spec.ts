import { test } from '@playwright/test'

test.describe('Admin Login Flow', () => {
  test.fixme('redirects /admin to /admin/login when not authenticated', async () => {})
  test.fixme('shows login form with email and password fields', async () => {})
  test.fixme('shows error message with invalid credentials', async () => {})
  test.fixme('redirects to /admin after successful login', async () => {})
})
